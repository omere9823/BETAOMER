<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.betaomer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Credits"></activity>
        <activity
            android:name=".PositionsStatus"
            android:label="@string/title_activity_positions_status"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".YourPosition" />
        <activity android:name=".Choice" />
        <activity android:name=".Worker" />
        <activity android:name=".Createevent" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Loginok" />
        <activity android:name=".Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>